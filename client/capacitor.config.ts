import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pwc.PostureDetector',
  appName: 'Posture_Detector',
  webDir: 'build'
};

export default config;
